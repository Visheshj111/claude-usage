It tracks claude usage and can export the chats, it is a chrome extension dude.

-> Used simple typescript, Javascript for backend. Html and css for the UI. 
-> Need support for the in screen UI right now, see if you can help. 

visit /src/backend for the backend files. (other folder have backend files too, but they are limited to that section)
visit .css and .html files for the UI. 
